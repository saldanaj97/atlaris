# Daily Recap — 2026-04-17

## Focus

April 17 was a focused dead-code and knip cleanup day kicked off by scoping the
`codebase-cleanup-2026-04-16` plan:

- Dead-code sweep — unused components, helpers, fixtures, and types
- Tracked-plans hygiene — make the new `.plans/codebase-cleanup-2026-04-16/` tree committable
- Knip follow-through — drop the re-export surface that the component removal left behind

There was no new feature work on April 17; the entire day was about shrinking
the surface that the April 14–16 refactors had left unused.

## Dead-Code Sweep + Cleanup Plan Scope

Primary commit: `b7fc6a99` `chore: remove unused components and types`

What shipped:

- Added the `.plans/codebase-cleanup-2026-04-16/` tree covering eight parallel tracks:
  - `00-meta-and-tooling.md`
  - `01-dedup-dry.md`
  - `02-type-consolidation.md`
  - `03-dead-code.md`
  - `04-circular-deps.md`
  - `05-weak-types.md`
  - `06-defensive-code.md`
  - `07-deprecated-legacy.md`
  - `08-ai-slop-comments.md`
  - `99-cross-track-coordination.md` and `README.md`
- Introduced `knip.jsonc` so knip has a stable config for the cleanup work that follows.
- Removed unused UI and dashboard components that no longer had live consumers:
  - `src/app/dashboard/components/ActivityStream.tsx`
  - `src/app/dashboard/components/ActivityStreamHeader.tsx`
  - `src/app/dashboard/components/QuickStats.tsx`
  - `src/app/plans/[id]/components/PlanDetailsCard.tsx`
  - `src/app/plans/[id]/components/PlanDraftView.tsx`
  - `src/app/plans/[id]/components/PlanModuleCard.tsx`
  - `src/app/plans/[id]/modules/[moduleId]/components/TaskCard.tsx`
  - `src/app/plans/components/UpgradeCTACard.tsx`
  - `src/components/shared/nav/LandingNavigation.tsx`
- Pruned dead helpers and types across plan, PDF, scheduling, AI, billing, and job modules:
  - `src/features/plans/api/preflight.ts` (deleted)
  - `src/features/plans/validation/learningPlans.ts` (trimmed)
  - `src/features/pdf/validation/pdf.ts` (trimmed)
  - `src/features/jobs/constants.ts`, `src/features/jobs/types.ts`
  - `src/features/ai/generation-policy.ts`, `src/features/ai/types/provider.types.ts`
  - `src/features/billing/tier-limits.types.ts`
  - `src/features/scheduling/scheduling.types.ts`
  - `src/lib/config/env.ts`, `src/hooks/streaming/parse-sse-plan-event.ts`
- Dropped stale test fixtures and mocks:
  - `tests/fixtures/module-access.ts`
  - `tests/fixtures/plan-access.ts`
  - `tests/mocks/shared/auth-session.ts`
- Refreshed `docs/development/commands.md` and `.github/copilot-instructions.md` with the updated tooling surface.

Why it mattered:

- The April 14–16 refactors had consolidated real behavior behind feature-owned boundaries, which left a long tail of UI components, helpers, and fixtures with zero live consumers.
- Killing that tail in one focused pass made the remaining surface match what the app actually ships and gave `knip` a clean baseline to enforce going forward.

Scale note:

- 55 files touched, roughly 1,500 net lines removed from app/feature/test code (the +1,374 figure in the diff is mostly the new cleanup plan docs and lockfile churn).

## Tracked-Plans Hygiene

Primary commit: `be1d4389` `chore: remove plans directory from .gitignore`

What shipped:

- Removed the `plans/*` entry from `.gitignore` so the new `.plans/codebase-cleanup-2026-04-16/` tree could be tracked.

Why it mattered:

- The cleanup plan from the morning commit only works as a shared artifact if it lives in the repo; without this change, the whole track plan would have stayed untracked by default.

## Knip Follow-Through

Primary commit: `008bc275` `chore(knip): remove leftover exports and duplicate aliases`

What shipped:

- Dropped exports from `src/app/plans/[id]/helpers.ts`, `src/app/plans/components/plan-utils.ts`, `src/app/settings/integrations/components/index.ts`, and `src/features/billing/tier.ts` that no longer had external callers.
- Collapsed duplicate `service-role`, `request-context`, and tier aliases:
  - `src/lib/db/service-role.ts`
  - `src/lib/logging/request-context.ts`
  - `src/features/billing/tier.ts`
- Updated `knip.jsonc` to silence duplicate-alias noise for the intentional title-length schema constants while letting the rest of the report stay honest.
- Lined up smaller consumer-side fixes in `src/app/api/internal/jobs/regeneration/process/route.ts`, `src/app/dashboard/components/activity-utils.ts`, `src/app/plans/new/components/usePdfExtractionDraft.ts`, `src/app/pricing/components/PricingCard.tsx`, `src/app/pricing/components/PricingTiers.tsx`, `src/app/settings/ai/components/ModelPreferencesSelector.tsx`, and `src/features/billing/usage-metrics.ts`.
- Refreshed `docs/development/environment.md` and `docs/development/logging.md` to match the narrower import surface.

Why it mattered:

- The morning's component/helper removal left behind a set of re-exports and aliases that knip flagged as unused once their consumers were gone.
- Resolving them the same day kept the knip baseline green so the multi-track cleanup work the following day started from a clean report.

## Outcome

By the end of April 17, the repo had:

- a concrete eight-track cleanup plan committed at `.plans/codebase-cleanup-2026-04-16/`,
- a first real dead-code sweep across components, helpers, fixtures, and types,
- and a stable knip baseline with the leftover aliases and re-exports removed.

The common theme was shrinking the surface: the refactor days made the architecture
clearer, and April 17 made the codebase match that architecture by deleting the
code that was no longer on any live path.
