# Daily Recap — 2026-04-22

## Focus

April 22 was tooling and architecture hardening:

- Biome / hook / CI alignment
- Regeneration orchestration boundary extraction for issue #309
- `job_queue` client-write hardening
- Agent-context documentation updates

## Tooling, Hooks, And Biome CI

Primary commits:

- `9dac7730` `chore(vscode): remove TypeScript default formatter setting`
- `c615c581` `chore(biome): update configuration and linter rules`
- `ae646e96` `chore(package): update type checker configuration in package.json`
- `79b3bcb0` `Consolidate hooks to Husky and add Biome CI`

What shipped:

- Standardized formatting/lint ownership around Biome.
- Moved pre-commit behavior onto Husky / lint-staged and removed the old
  pre-commit config path.
- Added PR CI lint coverage so Biome has a real automation gate instead of
  relying on local convention.
- Documented command surface updates in README / development docs.

Why it mattered:

- The repo had multiple overlapping local enforcement paths. This collapsed the
  active hook path and made CI responsible for the same formatting/lint contract
  developers see locally.

## Regeneration Orchestration Boundary

Primary commits:

- `d89ce66a` `refactor(regeneration): deepen orchestration boundary`
- `cbdefe78` `docs: update agent context learnings`

Plan source:

- `.plans/309-rfc-deepen-regeneration-orchestration-boundary/`

What shipped:

- Added `src/features/plans/regeneration-orchestration/` as the boundary for
  enqueue and worker processing semantics.
- Reduced the regenerate route to HTTP/auth parsing and response mapping.
- Reduced the worker to drain-loop and inline-drain primitives.
- Moved payload schema, request orchestration, job processing, quota seam use,
  rate-limit handling, and missing/wrong-owner collapse behind the boundary.
- Added boundary-level unit coverage and slimmed route/worker tests around the
  new ownership split.

Why it mattered:

- Regeneration had route, worker, quota, queue, and inline-drain behavior tied
  together. The new boundary gives that workflow one owner and makes future
  queue / worker changes less likely to back-import HTTP or worker details.

## `job_queue` Client-Write Hardening

Primary commit:

- `74ff2a67` `security(db): harden job_queue client writes`

What shipped:

- Added migrations `0028_harden_job_queue_service_role_writes.sql` and
  `0029_harden_job_queue_anonymous.sql`.
- Tightened `job_queue` grant posture so writes stay service-role owned.
- Updated bootstrap / RLS helpers and security tests to check drift for the
  affected grants.

Why it mattered:

- Queue writes are worker/service infrastructure, not client-owned data
  mutations. The database permissions now match that contract.

## Outcome

By end of April 22, the repo had a clearer local/CI formatting contract, a
dedicated regeneration orchestration package for issue #309, and a tighter
database privilege posture around `job_queue`.
