# Daily Recap — 2026-04-23

## Focus

April 23 was the heaviest architecture day in this window:

- Plan read-projection boundary for issue #310
- Stripe write-side reconciliation ownership for issue #311
- Request-boundary route migration for issue #312
- Task-progress boundary for issue #313
- Plan evidence and follow-up documentation

## Plan Read Projection Boundary

Primary commits:

- `fe68e314` `feat(plans): canonicalize plan read-projection boundary (#310)`
- `edacea56` `docs: refine agent context learnings on plan semantics and status derivation`
- `ee9c67b4` `refactor(plans): move read status types into feature layer`

Plan source:

- `.plans/310-rfc-canonicalize-plan-read-projection-boundary/`

What shipped:

- Created `src/features/plans/read-projection/` as the canonical owner for plan
  summary, detail, status, attempt, and display-status projection semantics.
- Deleted the old mixed read-service / read-models / status-read split.
- Moved paused/staleness display-status logic into the feature boundary and
  kept app/dashboard helpers as consumers.
- Added plan API contract coverage under
  `tests/integration/api/plans/plans-read.contract.spec.ts`.

Why it mattered:

- App and API consumers were answering one read concern through several
  competing plan modules. The new boundary makes plan-read truth easier to test
  and harder to drift.

## Stripe Write-Side Reconciliation

Primary commits:

- `a91b35c3` `feat(billing): consolidate Stripe write-side reconciliation (#311)`
- `c2d86842` `test(billing): refresh Stripe reconciliation and commerce specs`
- `97f87a38` `chore(plans): capture issue 311 billing reconciliation closure`

Plan source:

- `.plans/311-rfc-deepen-write-side-billing-reconciliation-ownership/`

What shipped:

- Added `stripe-commerce/reconciliation.ts` and
  `stripe-commerce/subscription-db-sync.ts` as the write-side owner for verified
  webhook application and local synthetic replay.
- Removed the free-standing `stripe-webhook-processor.ts`,
  `account-transitions.ts`, and dead public subscription helpers.
- Routed local checkout replay through the same write-side owner instead of
  assembling internals in the route layer.
- Re-centered tests on the new reconciliation seam and route behavior.

Why it mattered:

- Billing write semantics now live under the Stripe commerce boundary instead
  of split helper graphs. That lowers the risk of webhook and local replay paths
  drifting apart.

## Request Boundary Route Migration

Primary commits:

- `22d24c98` `feat(api): migrate v1 routes onto requestBoundary`
- `e8ab5a38` `test(api): expand requestBoundary and resources route coverage`
- `f686e8d8` `docs(api): document requestBoundary rate limits`
- `e5e094b6` `chore(plans): add RFC packages and ignore testcontainers env dumps`

Plan source:

- `.plans/312-rfc-migrate-api-routes-onto-request-boundary/`

What shipped:

- Extended `requestBoundary.route` to support authenticated route work with
  actor, params, request-scoped DB, owned access, and route-owned preflight.
- Migrated representative v1 routes across plans, resources, user, and Stripe
  surfaces.
- Deleted `withAuthAndRateLimit` after remaining route consumers moved to
  `requestBoundary.route`.
- Added request-boundary and resources route coverage.

Why it mattered:

- Route code now gets request DB, actor identity, and rate-limit behavior from
  one boundary instead of manually stitching auth wrappers and `getDb()` calls.

## Task Progress Boundary

Primary commits:

- `4081f211` `refactor(plans): consolidate task progress boundary`
- `c49db78b` `docs(plans): record issue 313 completion evidence`
- `871c1f4b` `chore(vscode): use biome as tsx formatter`

Plan source:

- `.plans/313-rfc-consolidate-plan-task-progress-boundary/`

What shipped:

- Added `src/features/plans/task-progress/` with boundary, visible-state, types,
  and public barrel.
- Moved plan/module task-progress writes behind `applyTaskProgressUpdates`.
- Moved UI derivation for stats, module state, lesson locks, and module
  completion into feature-owned pure helpers.
- Added unit and integration coverage for validation, ownership/scope
  rejection, revalidation paths, visible state, and UI-facing derivation.

Why it mattered:

- Plan and module actions now share one write contract for progress updates,
  while UI components consume derived state instead of owning business rules.

## Other Commits

- `6330f81e` `chore: update TypeScript SDK path in VSCode settings`
- `c1069957` `Delete Biome hooks CI plan and todos`

## Outcome

By end of April 23, the repo had four issue-backed architecture slices either
implemented or evidenced: #310 read projection, #311 billing reconciliation,
#312 request boundary migration, and #313 task progress. The common theme was
ownership: each workflow now has a narrower home and tests closer to that home.
