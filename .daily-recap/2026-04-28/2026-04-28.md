# Daily Recap — 2026-04-28

## Focus

April 28 was UI polish plus the Biome to Oxlint / Prettier migration:

- Tooltip and analytics surface polish
- Billing portal component extraction
- Oxlint / Prettier tooling migration
- Formatter sweep across app and tests
- Early cleanup after the read-projection refactor

## Product UI Polish

Primary commits:

- `7f772df3` `feat(ui): add tooltip component and enhance UI consistency`
- `c382ab7d` `feat(analytics): enhance achievements and usage analytics pages with dynamic icons`
- `db0ef5ef` `feat(billing): implement ManageSubscriptionButton component and update imports`

What shipped:

- Added the shared tooltip provider and tooltip component wiring.
- Updated auth controls and achievement cards to use the shared tooltip
  behavior.
- Refined typography and product-page global styles.
- Updated achievements and usage analytics pages with dynamic icon rendering.
- Extracted `ManageSubscriptionButton` so billing portal behavior has a
  dedicated component and focused tests.

Why it mattered:

- These changes tightened repeated product UI patterns instead of leaving small
  interaction and billing behaviors scattered across page components.

## Tooling Migration

Primary commits:

- `8d401cb1` `chore(tooling): replace biome with oxlint and prettier`
- `668afe9c` `style(app): reformat app source for prettier`
- `08e3318b` `test(tests): reformat suite and fix AuthControls test`
- `43b12069` `chore(package): remove format check from CI and update check:full script`
- `d049d320` `chore(config): add Fallow configuration and remove Knip settings`

What shipped:

- Replaced Biome with project-local Oxlint and Prettier.
- Updated CI, local hooks, package scripts, lockfile state, and docs for the
  new lint / format split.
- Reformatted app, feature, and test source under the new Prettier rules.
- Fixed the `AuthControls` test so it matches the runtime tooltip provider
  requirement.
- Removed Knip configuration and introduced Fallow configuration for unused-code
  checks.
- Removed the Prettier format check from CI while keeping local formatting
  ownership.

Why it mattered:

- The repo stopped carrying two competing cleanup stories. Oxlint / Prettier
  took over day-to-day enforcement, while Fallow became the explicit unused-code
  audit tool.

## Follow-Up Cleanup

Primary commits:

- `205bea50` `refactor(plans): reorganize imports and update read-projection references`
- `3c456964` `refactor(tests): streamline help message printing for runners`

What shipped:

- Repointed imports after the read-projection package moved.
- Removed obsolete index files and adjusted tests to match the new import
  shape.
- Centralized Vitest runner help-message printing for unit, integration, and
  security runners.

Why it mattered:

- This kept the prior architecture work from leaving stale import paths and
  duplicated runner help text behind.

## Outcome

By end of April 28, the product UI had cleaner shared interaction patterns and
the repository had moved from Biome / Knip to Oxlint / Prettier / Fallow with
the matching app and test formatting sweep complete.
