# Daily Recap — 2026-04-30

## Focus

April 30 was a dense plan-generation and boundary cleanup day:

- Dashboard/list attempt-cap correctness
- Plan lifecycle import and utility cleanup
- Summary-status extraction
- Retry-policy and regeneration simplification
- Stream, billing catalog, RLS, and request-boundary cleanup
- Test helper consolidation

## Plan Status And Lifecycle Cleanup

Primary commits:

- `25d2f183` `fix(plans): align list/dashboard attempt cap with reservation policy`
- `602a5e3c` `refactor(plans): remove convenience barrels and streamline imports in plan components`
- `590c6b22` `feat(plans): implement codebase simplification and introduce new utility functions`
- `185cf8d4` `refactor(plans): extract canonical summary status module`

What shipped:

- Fixed list and dashboard display status so attempt-cap handling follows the
  same environment-backed reservation policy as the server.
- Removed deprecated convenience barrels from lifecycle and
  generation-finalization imports.
- Added utility helpers for plan calendar dates and generation input-field
  construction.
- Moved canonical summary-status derivation into its own module and adjusted
  selectors and tests around it.

Why it mattered:

- The UI and lifecycle service stopped answering retry / failed status through
  slightly different rules. That reduces the chance of showing a failed plan
  while the server can still reserve another attempt.

## Regeneration And Retry

Primary commits:

- `0be24d42` `feat(jobs): centralize retry policy in shared module`
- `50fb9ea5` `refactor(plans): simplify regeneration retry cleanup`
- `313e3e59` `docs(plans): close issue 318 observability plan`
- `4bfbf2f3` `docs(plans): add plan package for queue decideJobRetry dedup`
- `3410762d` `test(plans): shared regeneration orchestration deps helper`

What shipped:

- Moved retry policy into shared code and wired regeneration / AI routing to
  the shared policy where appropriate.
- Simplified regeneration retry cleanup so retry outcomes are derived from the
  persisted queue row.
- Recorded observability and retry-dedup plan evidence.
- Added shared regeneration orchestration dependency builders for tests.

Why it mattered:

- Retry decisions became less duplicated, and the tests gained shared builders
  without making production code pay for test abstractions.

## Stream, Billing, RLS, And API Boundaries

Primary commits:

- `4e152b3e` `refactor(plans): split plan generation session into command, transport, cleanup`
- `c0e3455a` `test(plans): cover stream requestId and share event lookup helper`
- `3adfe01a` `feat(billing): centralize catalog read for pricing`
- `b4352024` `refactor(db): clarify RLS JWT replay and DB seam guardrails`
- `bcc93e29` `refactor(api): simplify request boundary composition`
- `eaee9d3b` `test(integration): align usage metrics month with getCurrentMonth`

What shipped:

- Split plan generation session handling into command preparation, SSE
  transport, and cleanup policy.
- Added request-id coverage for stream errors and shared event lookup helpers.
- Centralized billing catalog reads for pricing with injectable Stripe deps and
  local catalog parity.
- Clarified RLS JWT replay naming and guardrails.
- Simplified authenticated API routes around direct `requestBoundary.route`
  usage and updated the error-contract tests.
- Aligned usage metrics integration coverage with the current-month helper.

Why it mattered:

- Several high-churn boundaries became more explicit: streaming owns transport,
  billing owns catalog reads, RLS replay is named for what it does, and API
  routes delegate error serialization consistently.

## Outcome

By end of April 30, the plan-generation and regeneration internals were less
coupled, billing pricing reads had a single owner, and dashboard retry status
matched the reservation policy instead of a hardcoded cap.
