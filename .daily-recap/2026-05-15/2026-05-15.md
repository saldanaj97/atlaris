# Daily Recap - 2026-05-15

## Focus

Today’s git activity centered on shipping the lesson-generation stack and moving Supabase migrations into GitHub Actions.

## Work Completed

- `bb47de7b` `Add tier-based fallback routing for AI models`
  - Threaded user tier through model resolution and provider factories in `src/features/ai/ai-models.ts`, `src/features/ai/model-resolver.ts`, `src/features/ai/providers/factory.ts`, `src/features/ai/providers/openrouter.ts`, and `src/features/ai/providers/router.ts`.
  - Added fallback routing coverage in the AI provider and model resolver tests.

- `f421b088` `addressed coderabbit review comments`
  - Adjusted `src/features/ai/ai-models.ts` and `src/features/ai/providers/openrouter.ts`.
  - Added/updated tests in `tests/unit/ai/ai-models.spec.ts` and `tests/unit/ai/providers/openrouter.spec.ts`.

- `ce527e73`, `c8252084`, `498b94ea`, `ba189613`, `da5cc434`, `2c799851`
  - Built the lesson-content pipeline end to end: schema and enum additions in `supabase/migrations/0032_acoustic_lila_cheney.sql`, `src/shared/schemas/lesson-content.schemas.ts`, and `src/shared/types/lesson-content.types.ts`.
  - Added lesson-generation quota tracking in `src/features/billing/usage-metrics.ts`, `src/features/billing/metered-reservation.ts`, and `src/shared/constants/tier-limits.ts`.
  - Added provider support and orchestration for module lesson batches in `src/features/ai/providers/mock.ts`, `src/features/ai/providers/openrouter.ts`, `src/features/ai/providers/router.ts`, and `src/lib/db/queries/module-lesson-generation.ts`.
  - Added the on-demand generation route and UI states in `src/app/api/plans/[moduleId]/lesson-content/generate/route.ts`, `src/app/plans/[planId]/[moduleId]/components/LessonAccordionItem.tsx`, and `src/app/plans/[planId]/[moduleId]/components/ModuleLessonsClient.tsx`.
  - Follow-up review fixes landed in the same lesson-content slice with expanded unit and integration coverage.

- `a1bbd656`
  - Switched Supabase migrations to GitHub Actions with new workflow files at `.github/workflows/staging-db-migrations.yaml` and `.github/workflows/production-db-migrations.yaml`.
  - Updated the CI/CD and developer docs in `docs/ci/` and `docs/development/` to match the new migration contract.

- `8df455dc`
  - Removed the unused `@sentry/browser` dependency from `package.json` and `pnpm-lock.yaml`.

- `f9c3ca62`
  - Merged `main` into `develop` to keep the branch current.

## Next-Day Tasks

- Sweep the lesson-generation flow for any remaining plan-detail or analytics consumers that still assume lesson content is pre-generated.
- Remove any leftover Drizzle drift-check references from scripts or docs that still point at the old migration path.
- Audit the migration workflow docs and repo metadata for any stale references to the pre-GitHub-Actions process.
