# Skip ggshield in CI or when not configured
if command -v ggshield >/dev/null 2>&1 && [ -n "${GITGUARDIAN_API_KEY:-}" ]; then
  ggshield secret scan pre-commit
else
  echo "⚠️  ggshield not configured, skipping secret scan (CI environment)"
fi
