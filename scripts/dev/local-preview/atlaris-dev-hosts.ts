/**
 * Confirmed Supabase project ref for the hosted atlaris-dev database used by
 * `pnpm dev:local-preview`.
 *
 * This is a non-secret project identifier (visible in `db.<ref>.supabase.co`).
 * Set it to the atlaris-dev project ref from the Atlaris Local Preview
 * 1Password Environment before first use.
 */
export const ATLARIS_DEV_SUPABASE_PROJECT_REF = 'wbdvgscqvnoxaaydkxkv';
