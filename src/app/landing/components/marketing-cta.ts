/**
 * Shared primary CTA styling for landing hero + final section so “Start Free Trial”
 * matches (radius, padding, weight, shadow).
 */
export const marketingPrimaryCtaClassName =
	'group h-auto rounded-lg px-8 py-4 font-semibold shadow-lg transition hover:shadow-xl';
