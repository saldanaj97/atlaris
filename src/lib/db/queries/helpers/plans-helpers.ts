/**
 * Helper functions for plan-related queries.
 *
 * Placeholder module: no helpers currently extracted from plans schema.
 * Add pure functions here when plan query logic needs extraction
 * (e.g. mapping, aggregation, or validation helpers).
 */
